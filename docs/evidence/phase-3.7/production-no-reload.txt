Phase 3.7 no-reload and history evidence
Date: 2026-09-13

Before the fix, live admin acceptance showed search, filter, sort, page-size and clear
updates retaining one navigation entry but using replaceState. Browser Back/Forward could
not reconstruct the intermediate finder states.

Fix applied:
- Admin and student fragment updates now use history.pushState for user-driven updates.
- popstate-driven loads continue without adding a new history entry.
- The existing AbortController/request-sequence protection remains unchanged.

Post-fix production validation on Worker 817546c8-11b5-4952-b232-15efcd6ae6f0:
- Search, filter and clear each retained one document navigation entry.
- Search, filter and clear each updated the URL and result fragment in place.
- Back reconstructed the prior filtered URL and selected control value.
- Forward reconstructed the later URL and selected control value.
- The header remained mounted and the navigation entry count remained one.

The pre-fix behavior is retained above as the production defect evidence that justified the
surgical change.
