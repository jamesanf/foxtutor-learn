FoxTutor Learn Phase 1.4 credential verification
Captured: 2026-09-12
Production mutation: none

The runtime exposed one API-token credential through `CLOUDFLARE_API_TOKEN`.
The token authenticated to the intended Foxlearningltd account and the
`foxtutor.org` zone, but it did not have the capabilities required for
Phase 1.4 production activation.

Safe read probes:
- Account read: PASS, HTTP 200
- Zone read: PASS, HTTP 200
- Worker list: PASS, HTTP 200
- Access application list: PASS, HTTP 200
- Access identity-provider list: PASS, HTTP 200
- Access policy list: PASS, HTTP 200
- D1 list: BLOCKED, HTTP 401, error 10000
- Workers Routes list: BLOCKED, HTTP 403, error 10000
- R2 bucket list: BLOCKED, HTTP 403, error 10000
- User details/membership discovery: BLOCKED by token permissions reported by Wrangler

Non-mutating write authorization probes using invalid `{}` payloads:
- Access application write: BLOCKED, HTTP 403, `auth.forbidden`, error 1010
- Access identity-provider write: BLOCKED, HTTP 403, `auth.forbidden`, error 1010

The existing local Wrangler OAuth session was also checked. It identifies the
same Foxlearningltd account and can read or provisionally validate Worker,
D1 and Workers Routes operations, but its reported scopes do not include the
required Zero Trust Access application, identity-provider or policy write
capabilities.

No D1 database, Worker, route, Access application, identity provider, policy,
DNS record or public-site resource was created or changed. No credential
value, OAuth secret, session cookie or private response body was recorded.

Determination: BLOCKED — the intended Access-write-capable `foxtutor build
token` was not available to this runtime. Production mutation must not begin
until that existing credential is exposed through the configured runtime path.

Phase 1.4 runtime recheck: 2026-09-12T14:47Z
-----------------------------------------------

The runtime was rechecked before any production mutation. The configured
`CLOUDFLARE_API_TOKEN` still identified as the previously blocked token
mechanism rather than the stated `cfat_...` account token. The token metadata
was not recorded.

Current direct REST results:
- Account read: PASS, HTTP 200
- Zone read: PASS, HTTP 200
- Worker list: PASS, HTTP 200
- Access applications, identity providers and policies read: PASS, HTTP 200
- D1 list: BLOCKED, HTTP 401, error 10000
- Workers Routes list: BLOCKED, HTTP 403, error 10000
- R2 bucket list: BLOCKED, HTTP 403, error 10000
- Access application write: BLOCKED, HTTP 403, `auth.forbidden`, error 1010
- Access identity-provider write: BLOCKED, HTTP 403, `auth.forbidden`, error 1010
- Access policy write: BLOCKED, HTTP 403, `auth.forbidden`, error 1010
- Worker script upload validation: BLOCKED, HTTP 403, error 10000
- D1 create validation: BLOCKED, HTTP 401, error 10000
- Workers Route create validation: BLOCKED, HTTP 403, error 10000

`npx wrangler whoami` confirmed that Wrangler used the same insufficient
environment token while the separate OAuth profile still exposed the prior
partial scopes. `npm run test:production` confirmed the expected pre-activation
state: public routes remained healthy, but `/learn` returned the public site
with HTTP 200 and no Learn `X-Robots-Tag` or robots meta tag.

Production result: BLOCKED. No Worker, D1 database, route, Access application,
identity provider, policy, DNS record, mail message or public-site resource
was created or changed during this recheck.

Runtime recheck after the stated credential handover: 2026-09-12T14:54Z
---------------------------------------------------------------------------

The process environment still contains `CLOUDFLARE_API_TOKEN`, but
`npx wrangler whoami` identifies it as a User API Token and the value is not
in the expected `cfat_...` account-token format. The token is therefore still
the previously blocked mechanism, not the stated `foxtutor build token`.

Fresh sanitized direct results:
- Account read: PASS, HTTP 200
- Zone read: PASS, HTTP 200
- Worker list: PASS, HTTP 200, four existing Workers
- Access application list: PASS, HTTP 200, empty
- Access identity-provider list: PASS, HTTP 200, empty
- Access policy list: PASS, HTTP 200, empty
- D1 list: BLOCKED, HTTP 401, error 10000
- Workers Routes list: BLOCKED, HTTP 403, error 10000
- R2 bucket list: BLOCKED, HTTP 403, error 10000
- Access application write validation: BLOCKED, HTTP 403, error 1010
- Access identity-provider write validation: BLOCKED, HTTP 403, error 1010
- Access policy write validation: BLOCKED, HTTP 403, error 1010
- D1 create validation: BLOCKED, HTTP 401, error 10000
- Workers Route create validation: BLOCKED, HTTP 403, error 10000

The existing Wrangler OAuth session still identifies the Foxlearningltd
account and can dry-run the Worker and list the two unrelated D1 databases,
but its scopes do not include Zero Trust Access writes. No production
mutation was attempted.

Local/public verification after this recheck:
- `npm test`: PASS, 6 files, 12 tests
- `npm run build`: PASS
- `npm run check`: PASS, Wrangler dry-run
- `npm run test:browser`: PASS
- Public `/`, `/robots.txt` and `/sitemap.xml`: HTTP 200 with body hashes
  matching the stored baseline
- `npm run test:production`: expected BLOCKED state; public routes pass and
  `/learn` remains the pre-activation public-site fall-through

The only code change in this pass hardens malformed cookie parsing so invalid
percent-encoded values are ignored rather than throwing before authorization.
No token, cookie, private response body or user data was recorded.
